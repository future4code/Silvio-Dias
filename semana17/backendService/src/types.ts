export type user = {
   id: string
   name: string
   nickname: string
   email: string
   CEP: string
}

export type address = {
   cep:string
   logradouro:string
   numero:number
   complemento:string
   bairro:string
   cidade:string
   estado:string
   id_usuario:string
}